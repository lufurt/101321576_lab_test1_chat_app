const express = require('express');
const router = express.Router();

// Placeholder
router.get('/rooms', (req, res) => {
    // Placeholder response
    res.json({ message: 'List of chat rooms' });
});

// Export the router
module.exports = router;
